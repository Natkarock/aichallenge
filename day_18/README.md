# 🧠 Weather MCP Agent + Multi-Chat UI

## 🌦️ Что делает проект

Это **Streamlit-приложение**, объединяющее два режима работы:

### 1. ⛅ **Погодный MCP агент**
MCP (Multi-Context Pipeline) агент — цепочка из нескольких функций (`LangGraph`, `LangChain`), выполняющихся пошагово с помощью инструментов.

#### 🔧 Основные шаги:
1. **model_call_with_tools** — анализирует запрос пользователя и решает, какие инструменты вызывать.  
2. **call_weather** — вызывает инструмент для получения погоды.  
3. **call_image_generation** — создаёт визуализацию прогноза.  
4. **call_pdf_to_markdown** — конвертирует PDF-отчёты.  
5. **call_final_answer** — объединяет результаты и формирует финальный ответ.

#### 📂 Результаты:
- текстовое описание прогноза  
- изображения  
- PDF отчёт  
- `thread_id` (идентификатор выполнения в чекпоинтере)

#### 🧩 Используемые технологии:
- **LangGraph** — построение графа выполнения  
- **LangChain** — LLM-интерфейсы  
- **OpenAI API**  
- MCP Tools — внешние функции (погода, изображения, PDF)

---

### 2. 💬 **Обычные чаты с долговременной памятью**

В боковой панели:
- «➕ Новый чат» — создаёт обычный чат без MCP.  
- Каждый чат хранится в `memory/chats.json`.  
- Промпт: _«Ты дружелюбный помощник.»_  
- Все истории сохраняются между перезапусками.

---

### 🗂 Структура проекта
```
project/
├─ main.py               # UI: MCP + чаты
├─ agent.py              # MCP-граф
├─ memory/
│  ├─ chats.json         # долговременная память чатов
│  ├─ *.pdf              # отчёты MCP
│  └─ *.sqlite / *.jsonl # чекпоинтер MCP
└─ requirements.txt
```

---

### 🚀 Запуск
```bash
pip install -r requirements.txt
streamlit run main.py
```

**Переменные окружения**
- `OPENAI_API_KEY`
- `OPENAI_MODEL` (по умолчанию `gpt-4.1-mini`)

**Для MCP чекпоинтера:**
```bash
pip install -U langgraph langgraph-checkpoint-sqlite
```

---

### 🧭 Пример работы MCP
```
Пользователь: "Какая погода завтра в Краснодаре?"

→ model_call_with_tools → определяет нужный инструмент
→ call_weather → получает прогноз
→ call_image_generation → создаёт иллюстрации
→ call_final_answer → формирует итоговый отчёт

Результат:
✅ Описание погоды
✅ Картинки
✅ PDF с прогнозом
```